# --- Core Framework & Configuration ---
fastapi>=0.100.0        # Web Framework
uvicorn[standard]       # ASGI Server
pydantic>=2.0.0         # Data Validation
pydantic-settings       # Environment variable management
python-dotenv

# --- Database & ORM ---
sqlalchemy>=2.0.0       # ORM
asyncpg                 # Async PostgreSQL Driver (used in your DATABASE_URL)
psycopg[binary]         # Required natively by Procrastinate's PsycopgConnector
alembic                 # Migrations

# --- Task Queue (Worker Pattern) ---
procrastinate           # Background task processing queue

# --- Vector & Graph Stores (Repository Pattern) ---
qdrant-client           # Qdrant Vector Database driver
neo4j                   # Neo4j Graph Database driver

# --- AI & Extractors (Strategy Pattern) ---
ollama                  # Local LLM Gateway for Embeddings & Chat
gliner                  # Semantic Entity Extractor
fastembed               # Generates BM25 Sparse Vectors locally for Qdrant
scikit-learn>=1.3.0     # Required for HDBSCAN clustering engine
numpy                   # Explicitly required for HDBSCAN matrix math

# --- Developer Tools (CLI) ---
typer[all]              # MISSING: Powers the 'rag' command
setuptools              # Required for 'pip install -e .' editable mode


# --- Testing & Utils ---
pytest
pytest-asyncio          # Required for your @pytest.mark.asyncio decorators
httpx
watchdog[watchmedo]>=4.0.0 # Hot-reloading for the background worker